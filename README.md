# SustainableML
This project explores the effects of inefficient ML practices and how they affect metrics such as runtime, system/gpu memory usage, and overall energy consumption. To run the project, first run `pip install -r requirements.txt` and then `python main.py`. 


